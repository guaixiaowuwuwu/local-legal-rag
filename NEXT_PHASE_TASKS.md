# 下一阶段任务清单

本阶段目标：把当前 RAG 骨架推进到“可本地演示、可导入真实资料、可稳定验证”的 MVP。

## 1. 环境与运行基线

- [x] 创建并固定本地虚拟环境安装流程。
- [x] 执行 `python3 -m pip install -e ".[ui,faiss,dev]"` 并记录依赖安装问题。
- [x] 修复或确认 `pytest` 可用，确保 `python3 -m pytest -q` 能运行。
- [x] 增加一条轻量端到端烟测命令，默认使用 `--llm-backend extractive`。
- [x] 明确 CPU、GPU、内存、模型路径等本地运行要求。

当前状态：

- 轻量开发环境使用 `.venv`、`python3 -m pip install --no-deps -e .` 和
  `python3 -m pip install pytest`，可运行 `python3 -m pytest -q`。
- 全量依赖安装命令已执行；本机未发现版本冲突，但 PyPI 下载速度过低，
  已在 README 记录 `numpy`/`chromadb` 下载问题和替代验证方式。
- 已新增 `legal-rag smoke` / `PYTHONPATH=src python3 -m legal_rag.cli smoke`，
  默认使用 `local` 向量库、`hash` Embedding 和 `extractive` 回答模式。

验收标准：

- `PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py' -v` 通过。
- `python3 -m pytest -q` 通过或 README 说明原因与替代命令。
- 示例文档可以完成加载、切分、建库、问答。

## 2. 知识库资料准备

- [x] 整理首批真实法律文本或内部制度文档。
- [x] 建立 `data/documents/` 的目录规范，例如按法律领域、效力层级或业务主题分类。
- [x] 记录每份资料的来源、版本、发布日期、适用范围。
- [x] 增加至少 5-10 个真实问答样例，用于检索效果回归。
- [x] 明确非正式示例资料与正式知识库资料的边界。

当前状态：

- 已新增 `data/documents/formal/labor/` 作为默认正式资料目录，并按法律领域与效力层级分类。
- 首批资料包含《中华人民共和国劳动合同法》《中华人民共和国劳动合同法实施条例》《中华人民共和国劳动法》《中华人民共和国社会保险法》《中华人民共和国劳动争议调解仲裁法》的官方文本摘录。
- `data/documents/FORMAL_SOURCES.md` 记录了来源、官方标识、公布日期、施行日期、制定机关和适用范围。
- `data/evaluation/labor_qa_examples.json` 提供 10 条劳动用工问答回归样例，每条标注期望来源文件和关键命中词。
- `data/documents/examples/` 仅存放演示资料；默认配置 `docs_dir: data/documents/formal` 不导入示例资料。
- 当前首批资料是“权威文本摘录版”，用于 MVP 检索回归，不代表完整正式法律库。

验收标准：

- 至少一批真实资料可以成功导入。
- 每个测试问题能追溯到具体来源片段。

## 3. 向量库与检索能力

- [ ] 使用 `extractive` 模式完成 Chroma 建库与问答烟测。
- [ ] 使用 FAISS 完成一次同样的建库与问答烟测。
- [ ] 比较 Chroma 与 FAISS 在本机上的速度、磁盘占用和检索结果。
- [ ] 为 `retrieve` 增加单元测试或可替代的假向量库测试。
- [ ] 设计空库、坏路径、无支持文件、损坏 PDF 等异常提示。

验收标准：

- `legal-rag ingest --reset --llm-backend extractive` 成功。
- `legal-rag ask "试用期工资有什么要求？" --llm-backend extractive` 能返回来源。
- 检索失败时有明确错误信息，而不是静默失败。

## 4. 前端工作台

- [ ] 升级当前 Streamlit 页面为本地知识库问答工作台。
- [ ] 增加建库状态、文档数量、切分片段数量、向量库位置展示。
- [ ] 增加来源卡片，展示文件名、页码、法条、相关度分数和片段预览。
- [ ] 增加配置区：Top-K、向量库类型、生成模式、文档目录、向量库目录。
- [ ] 增加友好的空状态与错误提示。
- [ ] 保持默认演示模式为 `extractive`，避免未配置 ChatGLM 时页面不可用。

验收标准：

- `streamlit run app.py` 可以完成建库和问答演示。
- 未配置本地大模型时，页面仍可用。
- 来源信息足够让用户判断回答依据。

## 5. 本地模型接入

- [ ] 明确 Embedding 模型的本地路径配置方式。
- [ ] 明确 ChatGLM 模型的本地路径配置方式。
- [ ] 验证 `LEGAL_RAG_EMBEDDING_MODEL` 指向本地路径时可以离线建库。
- [ ] 验证 `LEGAL_RAG_LLM_MODEL` 指向本地路径时可以本地生成。
- [ ] 记录模型加载失败、显存不足、CPU 太慢等常见问题的处理方式。

验收标准：

- README 有一段可复制的本地模型配置示例。
- 至少完成一次本地模型真实问答验证，或明确当前机器限制。

## 6. 测试与质量

- [ ] 为配置加载增加测试。
- [ ] 为文档加载增加测试，覆盖 Markdown、TXT，以及 PDF/Word 的可选依赖提示。
- [ ] 为 CLI 参数覆盖增加测试。
- [ ] 为 Prompt 约束增加更多防幻觉场景测试。
- [ ] 配置 `ruff` 并运行格式/静态检查。

验收标准：

- 核心测试覆盖加载、切分、Prompt、配置和 CLI。
- 质量检查命令写入 README。

## 7. 文档与交付

- [ ] 更新 README 的当前能力、限制和快速演示步骤。
- [ ] 增加“常见问题”章节。
- [ ] 增加“法律风险与免责声明”章节。
- [ ] 增加“数据更新与重建向量库”说明。
- [ ] 记录下一阶段不做的事情，例如登录、多用户权限、联网检索、复杂后台。

验收标准：

- 新成员按 README 可以在本地跑通演示。
- 用户能理解系统不能替代正式法律意见。

## 暂缓事项

- [ ] 登录与权限系统。
- [ ] 多租户或多人协作。
- [ ] 复杂 React 管理后台。
- [ ] 联网法律检索。
- [ ] 自动法律意见书生成。
- [ ] 生产部署与运维监控。
